/** @type {import("next").Metadata} */
export const metadata = {
  title: "Equipment - Kieran Gunessee Photography",
  description: "A list of my photography equipment.",
  keywords: [
    "photography equipment, landscape, astrophotography, fine art photography",
  ],
};

export default function Layout({ children }) {
  return <>{children}</>;
}
